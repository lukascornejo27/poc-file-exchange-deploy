apiVersion: v1
kind: Service
metadata:
  name: file-exchange-api-svc
  namespace: file-exchange
spec:
  selector:
    app: file-exchange-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer